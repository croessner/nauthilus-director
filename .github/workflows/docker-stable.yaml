---
name: Production Docker Build

"on":
  push:
    tags:
      - 'v*'

permissions:
  contents: read
  packages: write

jobs:
  build:
    uses: ./.github/workflows/docker-stable-build.yaml
    with:
      release_tag: ${{ github.ref_name }}
      build_reason: tag-push
