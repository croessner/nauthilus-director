# nauthilus-director proof of concept

This directory contains the previous proof-of-concept implementation.

It is kept as reference material while the real implementation is rewritten cleanly from the repository root. New production code should not import packages from this directory or preserve this package structure by default.

Useful material here includes early IMAP and LMTP handling, configuration loading, Docker examples and the original Nauthilus HTTP client experiment.
